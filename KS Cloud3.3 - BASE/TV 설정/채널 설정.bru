meta {
  name: 채널 설정
  type: http
  seq: 5
}

post {
  url: {{base_external_URL}}/restapi/set_stb_channel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Token}}
}

body:json {
  {
      "mem_id": "koolsign_admin",
      "stb_sn": "KSU20100384",
      "port": "hdmi1"
  }
}

settings {
  encodeUrl: true
}
