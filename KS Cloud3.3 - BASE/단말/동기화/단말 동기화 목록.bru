meta {
  name: 단말 동기화 목록
  type: http
  seq: 3
}

post {
  url: {{base_external_URL}}/restapi/get_stb_sync_list
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Token}}
}

body:json {
  {
     "mem_id": "koolsign_admin",
     "subject": "동기화"
  }
}

settings {
  encodeUrl: true
}
